/**
 * Node-only surface: the pi child-process bridge. Import from
 * `@pi-desktop/engine/main` (Electron main / tests), never from renderers.
 */
export * from './jsonl';
export * from './pi-bridge';
export * from './resolve-pi';
